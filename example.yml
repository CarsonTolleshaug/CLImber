shell: bash
endpoints:
  - name: List Files
    pattern: /ls/(.*)
    run: ls -la $1
    responses:
    - condition:
        exitCode: 0
      responseCode: 200
      responseBody: {
        "outputText": "{{stdout}}"
      }
    